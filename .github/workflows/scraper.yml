name: Scrape Newspaper Front Pages

on:
  schedule:
    # Runs daily at 07:00 AM UTC
    - cron: '0 7 * * *'
  workflow_dispatch:

# Grants write access to the repository so the bot can commit updated images and YAML
permissions:
  contents: write

jobs:
  scrape-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Run Scraper and Data Generator
        run: python generate_data.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/
          git status
          git diff-index --quiet HEAD || git commit -m "Automated update: Newspaper front pages & Jekyll data ($(date -u +'%Y-%m-%dT%H:%M:%SZ'))"
          git push